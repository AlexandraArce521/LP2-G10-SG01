#include <iostream>
#include <string>
#include <vector>
#include <map>
using namespace std;
using namespace std;

class Administrador;
class Huesped;


class Huesped {
private:
    string nombre;
    int dni;
    string nacionalidad;

public:
    Huesped(string nombre, int dni, string nacionalidad)
    : nombre(nombre), dni(dni), nacionalidad(nacionalidad) {}
    
    
};

int main()
{
    vector <string> list1 = {
        "1. Usuario", 
        "2. Administrador"};
    int opcion1;
    int opcion2;
    
    vector <string> list2 = {
        "1. Registrar usuario",
        "2. Actualizar datos de usuario", 
        "3. Buscar habitación", 
        "4. Reservar habitacion", 
        "5. Cancelar reserva", 
        "6. Salir"};
    do{
        cout <<"========Menú======"<<endl;
        for (const string& opcion1 :  list1) {
            cout << opcion1 << endl;
        };
        cout << "Seleccione una opcion: ";
        cin >> opcion1;
        
        switch(opcion1) {
            case 1: 
            {
                for (const string& opcion2 :  list2) {
                    cout << opcion2 << endl;
                };
                cout << "[1]. Seleccione una opcion: ";
                cin >> opcion1;
                
                switch(opcion2) {
                    case 1: 
                }
                
                
                
                
                break;
            }
        }
        
    }while(opcion1 != 3);
    
}
